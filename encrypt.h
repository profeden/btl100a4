#pragma once


//ensure alpha characters only, remove numeric / special characters
// make lower case
// do translation on a character-by-character basis 
encrypt(intext, outtext)



// do the inverse translation on a character-by-character basis
decrypt(intext, outtext)

